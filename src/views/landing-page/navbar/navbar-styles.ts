import { createStyles } from "@material-ui/core";

export const NavStyles = () =>
  createStyles({
    formContainer: {
      position: "absolute",
      top: "2%",
      left: "10%",
      padding: "2rem"
    }
  });
